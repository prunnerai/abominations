name: Validate
on:
  push:
    paths: ['models/**/*.json']
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -c "import json; [json.load(open(f)) for f in __import__('glob').glob('models/*/config.json')]"
